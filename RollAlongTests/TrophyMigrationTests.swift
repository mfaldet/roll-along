//
//  TrophyMigrationTests.swift
//  RollAlongTests
//
//  S0-T1 STUB — pre-registered against the RollAlongTests target
//  (sprint-plan.md §2 S0-T1, §4a). The migration/backfill tests land in
//  S0-T4: three pre-trophy save fixtures (fresh / mid-progress / veteran,
//  built from real `ra_*` key dumps), idempotent backfill across
//  relaunches, legacy timestamps, and the per-badge boundary-parity rows
//  from trophy-catalog.md §6 (the legend-50 off-by-one audit). No tests
//  may live here before S0-T4.
//

import XCTest
@testable import RollAlong

final class TrophyMigrationTests: XCTestCase {}
